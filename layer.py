import numpy as np
import sigmoid_neuron

class Layer(self, layer_count, num_inputs):
    def __init__:
        self.neurons[]
        # Create the layer of neurons with random weights and biases 
        for i in range(layer_count): 
            self.neurons.append(sigmoidNeuron(num_inputs))


if __name__ == "__main__": # Run this testing code when layer file called directly 
    print("Hello, this is the direct file you are running from")
    input = Layer(5, 2)
    for i in input.nuerons.count(): 
        print(i, input.nuerons[i])
    pass
 